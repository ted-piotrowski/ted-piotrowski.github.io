import { Color } from '../../utils/styles';

interface Props {
    color?: Color;
}

const ConnectedIcon = (props: Props) => {
    const { color } = props;

    return (
        // todo
        null
    );
};

export default ConnectedIcon;